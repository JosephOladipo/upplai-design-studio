import { v2 as cloudinary } from 'cloudinary';
export const isCloudinaryConfigured = env => Boolean(env.CLOUDINARY_CLOUD_NAME && env.CLOUDINARY_API_KEY && env.CLOUDINARY_API_SECRET);
export function uploadMedia(buffer, mimeType, env = process.env) { if (!isCloudinaryConfigured(env)) return Promise.reject(Object.assign(new Error('Cloudinary is not configured.'),{code:'NOT_CONFIGURED'})); cloudinary.config({cloud_name:env.CLOUDINARY_CLOUD_NAME,api_key:env.CLOUDINARY_API_KEY,api_secret:env.CLOUDINARY_API_SECRET,secure:true}); return new Promise((resolve,reject)=>{const stream=cloudinary.uploader.upload_stream({folder:'upplai-design-studio/publishing',resource_type:'auto'},(error,result)=>error?reject(error):resolve({url:result.secure_url,publicId:result.public_id,resourceType:result.resource_type,format:result.format,bytes:result.bytes}));stream.end(buffer);}); }